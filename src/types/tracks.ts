export enum SortOption {
  NAME = "name",
  LOCATION = "location",
  START_DATE = "start_date",
}

export enum SortDirection {
  ASC = "asc",
  DESC = "desc",
}

export enum TrackStatus {
  CURRENT = "current",
  UPCOMING = "upcoming",
  PAST = "past",
  NO_DATE = "no-date",
}
